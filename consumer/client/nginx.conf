server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location /health {
    proxy_pass http://consumer-server:3002;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
